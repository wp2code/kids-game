- 采用 Composition API 的 `<script setup>` 语法编写 Vue 组件。
- 使用 Composables（如 `useAudio`, `useGameEngine`）封装业务逻辑并与 Pinia Store 交互。
- 通过定义 TypeScript 接口（`src/data/types.ts`）规范题目、分类及游戏状态的数据结构。
- 在 `vite.config.ts` 中手动实现 `.env` 文件解析以兼容 Vite 8 的环境变量加载机制。